name: Legacy build (disabled)

on:
  workflow_call:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy build workflow disabled; deployment is handled by pages-latest.yml"
